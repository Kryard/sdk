# Kryard Kotlin (Android) — future (Phase B)

Client-side embedded-wallet SDK for Android. **Not started** — gated on the
embedded-wallet/auth product (passkeys/StrongBox, sessions). Hand-written core (X-Stamp,
HPKE) + generated models from [`../../spec`](../../spec), tested against
[`../../vectors`](../../vectors).
