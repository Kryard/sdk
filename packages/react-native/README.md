# @kryard/react-native — future (Phase B)

Client-side embedded-wallet kit for React Native. **Not started** — gated on the
embedded-wallet/auth product (sub-orgs, passkeys, sessions). Mirrors Turnkey's
`react-native-wallet-kit`; sits on the TypeScript core.
