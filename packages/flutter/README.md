# Kryard Flutter — future (Phase B)

Client-side embedded-wallet SDK for Flutter (Dart). **Not started** — gated on the
embedded-wallet/auth product. Hand-written core (X-Stamp, HPKE) + generated models from
[`../../spec`](../../spec), tested against [`../../vectors`](../../vectors).
