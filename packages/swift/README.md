# Kryard Swift (iOS) — future (Phase B)

Client-side embedded-wallet SDK for iOS. **Not started** — gated on the embedded-wallet/
auth product (passkeys/Secure Enclave, sessions). A hand-written core (X-Stamp stamper,
HPKE) + generated models from [`../../spec`](../../spec), tested against
[`../../vectors`](../../vectors).
