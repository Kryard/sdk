{
  "name": "kryard-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Kryard SDKs — monorepo. Per-language clients for the Turnkey-compatible signing API and the managed EIP-7702 relay, driven by spec/ (OpenAPI) and verified against shared vectors/.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kryard/sdk.git"
  },
  "workspaces": [
    "packages/typescript"
  ],
  "scripts": {
    "build": "npm run build -w @kryard/sdk",
    "typecheck": "npm run typecheck -w @kryard/sdk",
    "test": "npm run test -w @kryard/sdk"
  }
}
